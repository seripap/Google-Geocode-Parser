const suffixList = require('./suffixList');
const suffixes = require('./suffixes');
const directionals = require('./directionals');
const directionalList = require('./directionalList');

module.exports = {
  suffixList,
  suffixes,
  directionals,
  directionalList,
};
